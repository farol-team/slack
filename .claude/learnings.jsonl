{"date":"2026-08-06","card":"i28","type":"pitfall","key":"grep-guard-path-prefix-cover","insight":"In this repo a `git grep -n` guard reports `path:line:text`, so a stale identifier inside a directory that is itself being renamed shows up as a survivor before the rename and silently stops being one after it — the filter must pin the identifier, never rely on the path prefix.","confidence":9,"files":["app/api/slack-manifest.test.ts","runner/crates/farol-core/src/config.rs"]}
